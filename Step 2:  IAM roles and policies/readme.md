### 1. **Create `iam.tf` File**

Create a file named `iam.tf` in your project directory with the following content to define IAM roles for EKS and RDS.
Refer "iam.tf"

### 2. **Apply the IAM Role Configuration**

Once the iam.tf file is created, apply the configuration by running the following command:

```bash
terraform apply
```
